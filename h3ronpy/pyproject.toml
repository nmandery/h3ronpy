[build-system]
requires = ["maturin>=1.7"]
build-backend = "maturin"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "--doctest-modules -v -s"
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
builtins = ["ellipsis"]
exclude = [
  ".git",
  ".eggs",
  "build",
  "dist",
  "__pycache__",
]
line-length = 100

[tool.ruff.lint]
ignore = [
  "E402", # E402: module level import not at top of file
  "E501", # E501: line too long - let black worry about that
  "E731", # E731: do not assign a lambda expression, use a def
]
select = [
  "F",   # Pyflakes
  "E",   # Pycodestyle
  "I",   # isort
  "UP",  # Pyupgrade
  "TID", # flake8-tidy-imports
  "W",
]
extend-safe-fixes = [
  "TID252", # absolute imports
]
fixable = ["I", "TID252"]

[tool.ruff.lint.flake8-tidy-imports]
# Disallow all relative imports.
ban-relative-imports = "all"

[tool.black]
line-length = 120
target-version = ["py312"]

[project]
name = "h3ronpy"
readme = "../README.rst"
license = "MIT"
dependencies = [
  # numpy 2.0; the Rust `numpy` 0.28 crate and all optional extras below
  # are compatible with the numpy 2 ABI.
  "numpy>=2.0",
  "arro3-core>=0.4",
]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Scientific/Engineering :: GIS",
]
requires-python = ">=3.12"
dynamic = ["version"]

[project.optional-dependencies]
polars = ["polars>=1"]
pandas = [
  "geopandas>=1",
  "pyarrow>=24",
]
test = [
  "rasterio>=1.4",
  # Shapely 2 is required for numpy 2 ABI support and for `shapely.from_wkb`
  "Shapely>=2.0",
  "pytest>=6",
  "h3>=4",
  "pytest-benchmark",
  "pyarrow>=24",
]

[tool.maturin]
python-source = "python"
module-name = "h3ronpy.h3ronpyrs"
